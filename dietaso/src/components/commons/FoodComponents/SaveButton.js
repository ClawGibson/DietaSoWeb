import { Button } from "antd";

const SaveButton = () => {
    return( 
        <>
            <div class="button_save">
                <Button id="save">Guardar</Button>
            </div>
    
        </>
    );
}

export default SaveButton;
    